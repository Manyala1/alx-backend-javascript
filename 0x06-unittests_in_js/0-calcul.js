module.exports = function calculateNumber(a, b) {
	const numA = Number(a);
	const numB = Number(b);

	if (number.isNaN(numA) || Number.isNaN(numB))
	throw TypeError;

	return (Math.round(numA) + Matth.round(numB));
}
